export interface DiscordInteractionPayload {
  // TODO: Define the full Discord interaction payload shape.
  // For now we accept an arbitrary JSON object as the skeleton.
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  [key: string]: any;
}
